[Watch Mode - Iteration {{ iteration_count }}] Goal: {{ goal }}
{{ exec_status }}

Terminal context below shows ONLY NEW content since last observation.
- Terminals with "[Content unchanged]" have no new activity
- Changed terminals show only the NEW command blocks (not repeated old content)

{{ context }}

Instructions:
1. Analyze the NEW content shown above
2. If relevant to "{{ goal }}", provide actionable suggestions
3. If nothing actionable, respond with EXACTLY: <NoComment/>
4. Keep responses concise - focus on actionable insights only
