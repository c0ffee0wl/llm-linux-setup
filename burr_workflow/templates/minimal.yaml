# Workflow: __NAME__
# Template: minimal
# Generated by: workflow create
#
# This is a minimal workflow template. Add steps to the 'main' job.
# Documentation: https://github.com/c0ffee0wl/llm-linux-setup/tree/main/burr_workflow

name: "__NAME__"
schema_version: "1.0"
description: "A minimal workflow"

# Define workflow inputs here
# inputs:
#   target:
#     description: "Target to process"
#     type: string
#     required: true

# Environment variables (optional)
# env:
#   OUTPUT_DIR: "./output"

jobs:
  main:
    steps:
      - name: "Hello World"
        id: hello
        run: echo "Workflow started"

      # Add more steps here. Examples:
      #
      # Shell command:
      # - id: scan
      #   run: nmap -sV ${{ inputs.target | shell_quote }}
      #   timeout: 300
      #
      # LLM extraction:
      # - id: analyze
      #   uses: llm/extract
      #   with:
      #     input: ${{ steps.scan.outputs.stdout }}
      #     prompt: "Extract key findings"
      #     schema:
      #       type: object
      #       properties:
      #         findings: { type: array, items: { type: string } }
      #
      # Conditional step:
      # - id: report
      #   if: ${{ steps.analyze.outputs.findings | length > 0 }}
      #   run: echo "Found ${{ steps.analyze.outputs.findings | length }} items"

# Cleanup (runs on success or failure)
# finally:
#   - run: echo "Workflow complete"
